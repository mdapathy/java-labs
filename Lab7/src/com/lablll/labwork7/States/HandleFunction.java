package com.lablll.labwork7.States;

/**
 * Interface that represents a function that handles the state
 */
public interface HandleFunction {
    /**
     * Function that is called to change the state
     * and to handle it
     */
    void handleState();
}
